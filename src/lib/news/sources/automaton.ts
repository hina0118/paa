import type { NewsSource } from '../types';

export const automatonSource: NewsSource = {
  id: 'automaton',
  name: 'AUTOMATON',
  feedUrl: 'https://automaton-media.com/feed/',
  siteUrl: 'https://automaton-media.com',
};
