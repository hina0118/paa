{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "amazon-session",
  "description": "Capability for the amazon-session window (order detail fetching). Allows amazon.co.jp pages to emit Tauri events so the on_page_load handler can pass HTML back to Rust.",
  "windows": ["amazon-session"],
  "remote": {
    "urls": ["https://*.amazon.co.jp/*"]
  },
  "local": false,
  "permissions": ["core:event:allow-emit"]
}
